import Foundation

// map

let numbers = [1, 2, 3, 4, 5, 6]

numbers.map { (e: Int) -> Int in
    return e * 10 - 48
}

let r = numbers.map({$0 + 1})
r

















numbers.map {$0 + 1}
numbers




















